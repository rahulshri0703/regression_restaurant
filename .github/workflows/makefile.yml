name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: configure
      run: apt update
      
    - name: configure2
      run: apt install python3-pip
      
    - name: configure3
      run: pip install tox
         
            
      
    - name : runner
      run : tox

 
